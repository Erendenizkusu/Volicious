# Volicious API — ortam değişkenleri (kopyala: .env)
# Yerel Supabase değerleri için: supabase status

SUPABASE_URL=http://127.0.0.1:44321
SUPABASE_SERVICE_ROLE_KEY=<supabase status → SERVICE_ROLE_KEY>

# Cache-miss isteklerinde gerekli. Yoksa yalnızca cache-hit yolu çalışır.
GOOGLE_PLACES_API_KEY=

# AI "öne çıkan özellikler" (yorum özetleme, OpenAI gpt-4o-mini). Yoksa highlights devre dışı.
OPENAI_API_KEY=

# ─── AI Kategori-Uyum skoru (opsiyonel, VARSAYILAN KAPALI — altın kural) ───
# >0 yaparsan üst-N mekân için yorum çekilip AI ile "özellikle bu kategoride iyi mi" skorlanır
# ve RealScore yeniden sıralanır. Review-fetch Google maliyeti doğurur → gelir gelince aç.
# N≈12 (limit) → görünen tüm liste yeniden sıralanır. Cache: (mekan, kategori), 7 gün.
CATEGORY_FIT_TOP_N=0
# Harmanlama tabanı: adjusted = realScore * (floor + (1-floor)*fit). 0.6 önerilir.
CATEGORY_FIT_FLOOR=0.6

PORT=8787
CELL_PRECISION=6

# ─── Önbellek tazeliği (TTL, gün) — maliyet kaldıracı, Google ToS max 30 ───
# Nearby arama sonuçları (grid hücresi) kaç gün taze sayılır. Uzatmak = aynı bölgeden tekrar
# gelen isteklerde Google'a gitmeme. Kapsamı düşürmez (her miss zaten tam 3x3 tiling yapar).
CACHE_TTL_DAYS=10
# AI öne çıkanlar ($0.025 Place Details "Atmosphere") önbelleği kaç gün taze.
HIGHLIGHTS_TTL_DAYS=14

# ─── Maliyet güvenliği (RELEASE.md § A / altın kural) ───
# Cihaz-başına GÜNLÜK ücretsiz istek; bitince reklam izle → +istek.
FREE_REQUESTS_PER_DAY=1
# Bir rewarded ad kaç ek istek kazandırır.
AD_GRANT_REQUESTS=1
# Global GÜNLÜK / AYLIK Google Places çağrı tavanı (sert maliyet koruması).
# ~$0.032/çağrı, cache-miss başına ~5 çağrı. Deploy'da trafik/bütçeye göre ayarla.
DAILY_GOOGLE_BUDGET=300
MONTHLY_GOOGLE_BUDGET=5000
